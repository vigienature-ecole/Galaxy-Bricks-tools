<tool id="columns-simple-operation" name="Simple operations">
<description>on columns</description>
    <requirements>
        <requirement type="package" version="1.11.4">r-data.table</requirement>
    </requirements>
    <command detect_errors="exit_code"><![CDATA[
        Rscript '$__tool_directory__/columns-simple-operation.R' '$input' '$inputFormula' '$inputName' '$add' '$output' ]]>
    </command>
    <inputs>
        <param format="tabular,csv" name="input" type="data" label="Input file" />
        <param name="inputFormula" type="text" value="c1 + c2" label="Formula" help="Identify columns with a c and the number of the column (e.g. c2 for the second column)" />
        <param name="inputName" type="text" value="" label="Name of the resulting column" />
        <param falsevalue="FALSE" label="Add column to original dataset" name="add" truevalue="TRUE" type="boolean" />
    </inputs>
    <outputs>
        <data format="tabular" name="output" from_work_dir="result"/>
    </outputs>
    <tests>
        <test> <!--FAILED-->
            <param name="input" value="testData"/>
            <param name="inputFormula" value="c1 + c2"/>
            <param name="inputName" value="test"/>
            <output name="output" value="result"/>
        </test>
    </tests>
    <help><![CDATA[

.. class:: infomark

This tool performs operations on columns:
 * it is possible to combine multiple columns (e.g. c1 * c2)
 * it is possible to transform one column using constants (e.g. c1 / 2)
 * it is possible to combine the operations described above within a formula

you can use elementary operations

"+" addition
"-" subtraction
"*" multiplication
"/" division

you can transform your data
"log(c2)"
"exp(c2)"


**Example**

input dataset ::

 "a"  "b"
  1    0
  2    5
  5    6
 10    2

Formula ::

c1 + c2

Result ::

 "result"
    1
    7
   11
   12

    ]]></help>
</tool>
